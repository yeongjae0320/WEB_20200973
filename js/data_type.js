let number = 5;
let str = '문자열 입력';	//""도 묶음 가능
let prime = 1.5123;
let is_ok = true;	//참
let is_not = false;	//거짓
let undefi;	//변수 이름만, 초기화 X
let empty = null;	//비어 있음

console.log(number, str, prime, is_ok, is_not);
console.log(undefi, empty);	//여러 개 출력